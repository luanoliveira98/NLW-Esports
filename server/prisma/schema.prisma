generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model Game {
  id        String @id
  title     String
  bannerUrl String
}

model Ad {
  id              String   @id
  name            String
  yearsPlayings   Int
  discord         String
  weekDays        Json
  hourStart       Int
  hourEnd         Int
  useVoiceChannel Boolean
  createdAt       DateTime
}
